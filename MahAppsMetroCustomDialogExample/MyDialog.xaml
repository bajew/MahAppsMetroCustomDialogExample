<dialogs:CustomDialog x:Class="MahAppsMetroCustomDialogExample.MyDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro" 
             xmlns:local="clr-namespace:MahAppsMetroCustomDialogExample"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <dialogs:CustomDialog.Resources>
        <Style TargetType="{x:Type dialogs:BaseMetroDialog}"></Style>
    </dialogs:CustomDialog.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Titel}"></TextBlock>
        <TextBlock Grid.Row="1" Text="{Binding Message}"></TextBlock>
        <Button Grid.Row="2" Content="OK" Click="Button_Click"></Button>
    </Grid>
</dialogs:CustomDialog>
